GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
# local runs the same regression suite on this machine; modal uses a remote sandbox.
EVAL_BACKEND=local
# Optional. Run `uv run modal setup` before enabling Modal.
MODAL_TOKEN_ID=
MODAL_TOKEN_SECRET=
# Optional tracing. Never put real vault credentials into this educational game.
LOGFIRE_TOKEN=

